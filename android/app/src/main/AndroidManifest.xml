<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mobile.payuni.id"
    android:versionCode="1"
    android:versionName="1.0">
    
    <!-- 16KB page size compatibility -->
    <uses-sdk android:minSdkVersion="21" android:targetSdkVersion="36" />
    
    <!-- 16KB page size specific permissions -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    
    <!-- Enable 16KB page size support -->
    <application
        android:name=".Application"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/LaunchTheme"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true"
        android:enableOnBackInvokedCallback="true">
        
        <!-- 16KB page size compatibility -->
        <meta-data
            android:name="android.app.lib_name"
            android:value="flutter" />
            
        <!-- Enable 16KB page size support -->
        <meta-data
            android:name="android.app.page_size_agnostic"
            android:value="true" />
            
        <!-- Package alignment for 16KB compatibility -->
        <meta-data
            android:name="android.app.package_alignment"
            android:value="16384" />
            
        <!-- ELF alignment for 16KB compatibility -->
        <meta-data
            android:name="android.app.elf_alignment"
            android:value="16384" />
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <!-- 16KB page size compatibility -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
                
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
            
        <!-- Flutter 16KB page size support -->
        <meta-data
            android:name="flutter.embedding.android.PageSizeAgnostic"
            android:value="true" />
            
        <!-- Flutter package alignment for 16KB -->
        <meta-data
            android:name="flutter.embedding.android.PackageAlignment"
            android:value="16384" />
            
        <!-- 16KB page size agnostic support -->
        <meta-data
            android:name="android.app.supports_16kb_pages"
            android:value="true" />
            
        <!-- Memory alignment for 16KB pages -->
        <meta-data
            android:name="android.app.memory_alignment"
            android:value="16384" />

    </application>
    
    <!-- 16KB page size permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- 16KB page size hardware features -->
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.camera.flash" android:required="false" />
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.wifi" android:required="false" />
    
    <!-- 16KB page size compatibility features -->
    <uses-feature android:name="android.hardware.16kb.page.size" android:required="false" />
    <uses-feature android:name="android.hardware.memory.16kb" android:required="false" />
    

</manifest>
